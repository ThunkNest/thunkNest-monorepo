server:
  port: ${PORT:2020}

spring:
  profiles:
    active: ${SPRING_PROFILE:local}
  application:
    name: auth-service
  management:
    endpoints:
      web:
        exposure:
          include: health, info, refresh